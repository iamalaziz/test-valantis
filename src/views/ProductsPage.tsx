import React from 'react';
import ProductsList from '../components/ProductsList';

const ProductsPage = () => {
  return (
    <React.Fragment>
      <ProductsList />
    </React.Fragment>
  );
};

export default ProductsPage;
